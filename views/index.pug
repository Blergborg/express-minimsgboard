extends layout

block content
  h1= title
  p Welcome to #{title}
  //- display each msg by looping through msgs array in index route

  ul
    each val in messages
      li=val.text
      li=val.user + " @ " + val.added